services:
  finapp-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - backend-net
    develop:
      watch:
        - path: .
          action: sync+restart
          target: /app

networks:
  backend-net:
    driver: bridge
